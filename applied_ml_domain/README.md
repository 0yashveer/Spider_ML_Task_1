##Instructions
-Download app.py , index.html, index.py
-ingest.py helps in ingesting files to the database, which is run once while cold start of the model.
-once vector collection is made , run ollama pull llama3:8b cmd in your terminal after downloading the mentioned model.
-run app.py and start asking your doubts.

#information 
-embedding model used is all-MiniLM-L6-v2
-llama3:8b model is used as llm
-chroma db is used as vector db

